# Visual Companion Guide

- **Purpose:** Use a browser-based server to show mockups, diagrams, and side-by-side comparisons.
- **Interaction:** The tool writes HTML fragments to a specific directory which are then rendered for the user.
- **Decision Rule:** Use the browser when content is visual (mockups, diagrams, design polish). Use the terminal for text (requirements, tradeoffs, API design).
